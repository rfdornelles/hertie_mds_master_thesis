Aqui está o código Python para extrair as informações da sentença e preencher o dataset:

```python
import re
import json

def extrair_informacoes(sentenca):
    # Inicializar as variáveis
    processo = None
    juiz = None
    sexo_juiz = None
    nome = None
    local = None
    maconha = False
    maconha_g = 0
    maconha_outras = False
    cocaina = False
    cocaina_g = 0
    crack = False
    crack_g = 0
    ecstasy = False
    ecstasy_g = 0
    lsd = False
    lsd_g = 0
    sentenca_resultado = None
    pena_base = None
    tot_pen = None
    tot_pen_meses = 0
    outras_drogas = False
    resultado_art_28 = False
    resultado_art_33 = False
    resultado_art_34 = False
    resultado_art_35 = False
    denuncia_art_33 = False
    denuncia_art_34 = False
    denuncia_art_35 = False
    flag_local_de_trafico = False
    flag_preso_no_momento_da_sentenca = False
    flag_confissao_informal = False
    flag_confissao = False
    flag_denuncia_anonima = False
    flag_denuncia = False
    flag_atitude_suspeita = False
    flag_divergencias_nos_relatos_dos_policiais = False
    flag_investigacao = False
    flag_interceptacao = False
    flag_mandado = False
    aval_antecedentes = False
    aval_conduta = False
    aval_personalidade = False
    aval_natureza = False
    aval_quantidade = False
    aval_variedade = False
    aval_circunstancias = False
    aval_consequencias = False
    aval_culpabilidade = False

    # Processar a sentença
    linhas = sentenca.split('\n')
    for linha in linhas:
        # Processar o processo
        match = re.search(r'Processo nº: (\d+)', linha)
        if match:
            processo = match.group(1)

        # Processar o juiz
        match = re.search(r'Juíza de Direito - (.*)', linha)
        if match:
            juiz = match.group(1)

        # Processar o sexo do juiz
        match = re.search(r'Masculino|Feminino', linha)
        if match:
            sexo_juiz = match.group(0)

        # Processar o nome do réu
        match = re.search(r'RÉU\(S/RÉ\): (.*)', linha)
        if match:
            nome = match.group(1)

        # Processar o local
        match = re.search(r'Via Pública|Comércio e serviços|Estádio|Favela - Viela|Invasão/Ocupação|Residência|Restaurante e afins|Terminal/Estação|Área não ocupada', linha)
        if match:
            local = match.group(0)

        # Processar a maconha
        match = re.search(r'maconha|skank|haxixe', linha)
        if match:
            maconha = True
            match = re.search(r'(\d+\.\d+) gramas', linha)
            if match:
                maconha_g = float(match.group(1))

        # Processar a cocaína
        match = re.search(r'cocaína', linha)
        if match:
            cocaina = True
            match = re.search(r'(\d+\.\d+) gramas', linha)
            if match:
                cocaina_g = float(match.group(1))

        # Processar o crack
        match = re.search(r'crack', linha)
        if match:
            crack = True
            match = re.search(r'(\d+\.\d+) gramas', linha)
            if match:
                crack_g = float(match.group(1))

        # Processar o ecstasy
        match = re.search(r'ecstasy', linha)
        if match:
            ecstasy = True
            match = re.search(r'(\d+\.\d+) gramas', linha)
            if match:
                ecstasy_g = float(match.group(1))

        # Processar o LSD
        match = re.search(r'lsd', linha)
        if match:
            lsd = True
            match = re.search(r'(\d+\