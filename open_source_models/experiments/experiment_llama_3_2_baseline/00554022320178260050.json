Aqui está o código em Python para extrair as informações de sentença judiciária:

```python
import re
import json

def extrair_informacoes(sentenca):
    # Cabeçalho
    cabecalho = re.search(r'Vistos.*', sentenca, re.IGNORECASE)
    if cabecalho:
        cabecalho = cabecalho.group()
    else:
        cabecalho = None

    # Relatório
    relatorio = re.search(r'Relatório:.*', sentenca, re.IGNORECASE)
    if relatorio:
        relatorio = relatorio.group()
    else:
        relatorio = None

    # Fundamentação
    fundamentacao = re.search(r'Fundamentação:.*', sentenca, re.IGNORECASE)
    if fundamentacao:
        fundamentacao = fundamentacao.group()
    else:
        fundamentacao = None

    # Parte dispositiva
    parte_dispositiva = re.search(r'Parte dispositiva:.*', sentenca, re.IGNORECASE)
    if parte_dispositiva:
        parte_dispositiva = parte_dispositiva.group()
    else:
        parte_dispositiva = None

    # Outras informações
    outras_informacoes = re.search(r'Outras informações:.*', sentenca, re.IGNORECASE)
    if outras_informacoes:
        outras_informacoes = outras_informacoes.group()
    else:
        outras_informacoes = None

    # Dados do réu
    dados_reu = {}
    if cabecalho:
        cabecalho = cabecalho.split('\n')
        for linha in cabecalho:
            match = re.search(r'^(.*)\s*(.*)$', linha, re.IGNORECASE)
            if match:
                nome_reu = match.group(1).strip()
                dados_reu[nome_reu] = {}
                dados_reu[nome_reu]['processo'] = match.group(2).strip()
                dados_reu[nome_reu]['juiz'] = re.search(r'Juiz: (.*)', linha, re.IGNORECASE).group(1).strip()
                dados_reu[nome_reu]['sexo_juiz'] = re.search(r'Sexo do Juiz: (.*)', linha, re.IGNORECASE).group(1).strip()

    # Extrair informações específicas
    if 'incursa no artigo 33, caput, da Lei n° 11.343/06' in sentenca:
        dados_reu['Mariana da Silva Lima'] = {}
        dados_reu['Mariana da Silva Lima']['maconha'] = re.search(r'80 quilogramas de maconha', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['maconha_g'] = re.search(r'80 quilogramas de maconha', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['cocaina'] = re.search(r'cocaina', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['cocaina_g'] = re.search(r'cocaina', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['crack'] = re.search(r'crack', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['crack_g'] = re.search(r'crack', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['ecstasy'] = re.search(r'ecstasy', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['ecstasy_g'] = re.search(r'ecstasy', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['lsd'] = re.search(r'lsd', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['lsd_g'] = re.search(r'lsd', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['sentenca'] = re.search(r'Procedente', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['pena_base'] = re.search(r'8a', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['tot_pen'] = re.search(r'8a 6m', sentenca, re.IGNORECASE)
        dados_reu['Mariana da Silva Lima']['tot_pen_meses'] = re.search(r'8a