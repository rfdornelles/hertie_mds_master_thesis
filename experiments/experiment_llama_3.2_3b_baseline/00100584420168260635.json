Aqui está o código em Python para extrair as informações de sentença:

```python
import re
import json

def extrair_informacoes(sentenca):
    # Cabeçalho
    cabecalho = re.search(r'Vistos|SENTENÇA', sentenca, re.IGNORECASE)
    if cabecalho:
        return None

    # Relatório
    relatorio = re.search(r'Relatório: (.*)', sentenca, re.DOTALL)
    if relatorio:
        relatorio = relatorio.group(1)
    else:
        return None

    # Fundamentação
    fundamentacao = re.search(r'Fundamentação: (.*)', sentenca, re.DOTALL)
    if fundamentacao:
        fundamentacao = fundamentacao.group(1)
    else:
        return None

    # Parte dispositiva
    parte_dispositiva = re.search(r'Parte dispositiva: (.*)', sentenca, re.DOTALL)
    if parte_dispositiva:
        parte_dispositiva = parte_dispositiva.group(1)
    else:
        return None

    # Informações adicionais
    informacoes_adicionais = re.search(r'Outras informações: (.*)', sentenca, re.DOTALL)
    if informacoes_adicionais:
        informacoes_adicionais = informacoes_adicionais.group(1)
    else:
        return None

    # Extrair informações
    informacoes = {
        'processo': re.search(r'Processo Digital nº: (.*)', sentenca, re.IGNORECASE).group(1),
        'juiz': re.search(r'Juiz(a): (.*)', sentenca, re.IGNORECASE).group(1),
       'sexo_juiz': re.search(r'Sexo do Juiz: (.*)', sentenca, re.IGNORECASE).group(1),
        'nome': re.search(r'Indiciado: (.*)', sentenca, re.IGNORECASE).group(1),
        'local': re.search(r'Local: (.*)', sentenca, re.IGNORECASE).group(1),
       'maconha': re.search(r'Maconha: (true|false)', sentenca, re.IGNORECASE).group(1) == 'true',
       'maconha_g': re.search(r'Maconha: (.*g)', sentenca, re.IGNORECASE).group(1) if re.search(r'Maconha: (.*g)', sentenca, re.IGNORECASE) else 0,
       'maconha_outras': re.search(r'Maconha: (true|false)', sentenca, re.IGNORECASE).group(1) == 'true',
        'cocaina': re.search(r'Cocaina: (true|false)', sentenca, re.IGNORECASE).group(1) == 'true',
        'cocaina_g': re.search(r'Cocaina: (.*g)', sentenca, re.IGNORECASE).group(1) if re.search(r'Cocaina: (.*g)', sentenca, re.IGNORECASE) else 0,
        'crack': re.search(r'Crack: (true|false)', sentenca, re.IGNORECASE).group(1) == 'true',
        'crack_g': re.search(r'Crack: (.*g)', sentenca, re.IGNORECASE).group(1) if re.search(r'Crack: (.*g)', sentenca, re.IGNORECASE) else 0,
        'ecstasy': re.search(r'Ecstasy: (true|false)', sentenca, re.IGNORECASE).group(1) == 'true',
        'ecstasy_g': re.search(r'Ecstasy: (.*g)', sentenca, re.IGNORECASE).group(1) if re.search(r'Ecstasy: (.*g)', sentenca, re.IGNORECASE) else 0,
        'lsd': re.search(r'LSD: (true|false)', sentenca, re.IGNORECASE).group(1) == 'true',
        'lsd_g': re.search(r'LSD: (.*g)', sentenca, re.IGNORECASE).group(1) if re.search(r'LSD: (.*g)', sentenca, re.IGNORECASE) else 0,
       'sentenca': re.search(r'Sentença: (.*)', sentenca, re.IGNORECASE).group(1),
        'pena_base': re.search(r'Pena base: (.*)', sentenca, re.IGNORECASE).group(1),
        'tot_pen': re.search(r'Total pena: (.*)', senten