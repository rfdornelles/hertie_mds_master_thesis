Aqui está o código Python para extrair as informações da sentença judicial e preencher o dataset:

```python
import re
import json

def extrair_informacoes(sentenca):
    # Inicializar as variáveis
    processo = None
    juiz = None
    sexo_juiz = None
    nome = None
    local = None
    maconha = False
    maconha_g = 0
    maconha_outras = False
    cocaina = False
    cocaina_g = 0
    crack = False
    crack_g = 0
    ecstasy = False
    ecstasy_g = 0
    lsd = False
    lsd_g = 0
    sentenca = None
    pena_base = None
    tot_pen = None
    tot_pen_meses = 0
    outras_drogas = False
    resultado_art_28 = False
    resultado_art_33 = False
    resultado_art_34 = False
    resultado_art_35 = False
    denuncia_art_33 = False
    denuncia_art_34 = False
    denuncia_art_35 = False
    flag_local_de_trafico = False
    flag_preso_no_momento_da_sentenca = False
    flag_confissao_informal = False
    flag_confissao = False
    flag_denuncia_anonima = False
    flag_denuncia = False
    flag_atitude_suspeita = False
    flag_divergencias_nos_relatos_dos_policiais = False
    flag_investigacao = False
    flag_interceptacao = False
    flag_mandado = False
    aval_antecedentes = False
    aval_conduta = False
    aval_personalidade = False
    aval_natureza = False
    aval_quantidade = False
    aval_variedade = False
    aval_circunstancias = False
    aval_consequencias = False
    aval_culpabilidade = False

    # Processar a sentença
    linhas = sentenca.split('\n')
    for linha in linhas:
        # Processar o cabeçalho
        if 'Vistos' in linha or 'SENTENÇA' in linha:
            continue

        # Processar o processo
        match = re.search(r'Processo Digital nº: (\d+)', linha)
        if match:
            processo = match.group(1)

        # Processar o juiz
        match = re.search(r'Juiz\(a\) de Direito: (.*?) Vistos', linha)
        if match:
            juiz = match.group(1)

        # Processar o sexo do juiz
        match = re.search(r'Sexo: (.*?) Vistos', linha)
        if match:
            sexo_juiz = match.group(1)

        # Processar o nome do réu
        match = re.search(r'DOUGLAS ALEXANDRE GUILHERME', linha)
        if match:
            nome = 'DOUGLAS ALEXANDRE GUILHERME'

        # Processar o local
        match = re.search(r'local: (.*?)\.', linha)
        if match:
            local = match.group(1)

        # Processar a maconha
        match = re.search(r'maconha: (True|False)', linha)
        if match:
            maconha = match.group(1) == 'True'
        match = re.search(r'maconha_g: (\d+)', linha)
        if match:
            maconha_g = int(match.group(1))
        match = re.search(r'maconha_outras: (True|False)', linha)
        if match:
            maconha_outras = match.group(1) == 'True'

        # Processar a cocaína
        match = re.search(r'cocaina: (True|False)', linha)
        if match:
            cocaina = match.group(1) == 'True'
        match = re.search(r'cocaina_g: (\d+)', linha)
        if match:
            cocaina_g = int(match.group(1))

        # Processar o crack
        match = re.search(r'crack: (True|False)', linha)
        if match:
            crack = match.group(1) == 'True'
        match = re.search(r'crack_g: (\d+)', linha)
        if match:
            crack_g = int(match.group(1))

        # Processar o ecstasy
        match = re.search(r'ecstasy: (True|False)', linha)
        if match:
            ecstasy =